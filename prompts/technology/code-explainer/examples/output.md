# Example Output: Plain-English Code Explainer

## Input Values Used
- **Programming Language:** Python
- **Code Snippet:** 
```python
def find_even_numbers(numbers):
    evens = []
    for num in numbers:
        if num % 2 == 0:
            evens.append(num)
    return evens
